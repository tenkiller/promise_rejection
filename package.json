{
  "name": "promise_rejection",
  "version": "1.0.0",
  "description": "promise rejections in different testing frameworks",
  "scripts": {
    "mocha": "mocha --bail --color test/mocha",
    "vitest": "vitest --bail 1 --dir test/vitest run"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tenkiller/promise_rejection.git"
  },
  "devDependencies": {
    "chai": "^5.1.0",
    "eslint": "^8.56.0",
    "eslint-plugin-mocha": "^10.3.0",
    "eslint-plugin-vitest": "^0.3.22",
    "mocha": "^10.3.0",
    "sinon": "^17.0.1",
    "vitest": "^1.2.2"
  }
}
